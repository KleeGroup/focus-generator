\{{!-- use in app/views/{{module}}/{{name}}ListView--}}
\{{#panel "{{module}}.{{name}}.title" }}
  \{{#result "{{module}}.{{name}}.title" }}
    <thead id="tableHeader">
      <tr>
        <!-- TODO: Add columns to display properties -->
      </tr>
    </thead>
    <tbody>
      \{{#each collection}}
      <tr>
	<!-- TODO: Add display for each column -->
      </tr>
      \{{/each}}
    </tbody>
  \{{/result}}
\{{/panel}} 
